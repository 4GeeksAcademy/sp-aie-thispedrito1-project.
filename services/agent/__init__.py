"""Agente de soporte con LangGraph (Parte 1): el RAG del Hito 7 como grafo trazado.

La recuperación y la generación siguen viviendo en data/pipelines/rag.py;
aquí solo están el grafo (graph.py), su ejecución trazada (tracing.py) y el
endpoint (router.py)."""
