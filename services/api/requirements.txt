# 0.115.14: primera de la serie 0.115 que admite la starlette>=0.46.2 de mcpauth.
fastapi==0.115.14
uvicorn[standard]==0.32.1
python-multipart==0.0.17
tinydb==4.8.2
email-validator==2.2.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-dotenv==1.0.1
resend==2.34.0
sqlmodel==0.0.24
psycopg2-binary==2.9.12
pandas==2.2.3
prefect>=3.4,<4
# Cola de tareas asíncronas (Ticket #DEV-55): Celery con Redis como broker y
# result backend, y Flower para monitorizar la cola durante el desarrollo.
celery[redis]==5.6.3
redis==6.4.0
flower==2.0.1
# Base de conocimiento RAG (Hito 7): cliente de la base vectorial Qdrant y SDK
# compatible con OpenAI para los modelos de embeddings y de generación que
# proporciona 4Geeks (URL base y claves por variables de entorno).
qdrant-client==1.16.1
openai==2.48.0
eval-type-backport==0.2.2; python_version < "3.10"
# Agente LangGraph (Parte 1): el flujo del RAG como grafo con estado,
# checkpointing y trace. Se mantiene 0.6.11 tras pasar el venv a Python 3.12:
# acepta el langchain-core 1.x que exige langchain-mcp-adapters.
langgraph==0.6.11
# Servidor MCP con OAuth (mcps/healthcore): SDK oficial de MCP (trae FastMCP en
# mcp.server.fastmcp y el transporte Streamable HTTP), MCP Auth como resource
# server OAuth 2.1 y el adaptador con el que el agente LangGraph consume el
# servidor como cliente. mcp<2 lo exige langchain-mcp-adapters; por eso no se
# instala el paquete `fastmcp` aparte (sus versiones nuevas usan mcp 2.x).
mcp>=1.24,<2
mcpauth==0.1.1
langchain-mcp-adapters==0.3.2
