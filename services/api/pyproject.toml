[project]
name = "healthcore-suppliers-api"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi==0.115.6",
  "uvicorn[standard]==0.32.1",
  "python-multipart==0.0.17",
  "tinydb==4.8.2",
  "email-validator==2.2.0"
]

[project.scripts]
seed = "seed:main"
