# Progress - HealthCore Digital

## Estado actual del desarrollo
- Ya existe un briefing estrategico consolidado del proyecto con objetivos, alcance, KPIs y plan por fases.
- Ya existe contexto tecnico documentado con stack actual, decisiones de arquitectura y restricciones.
- La app principal identificada esta en Next.js 16 + React 19 + TypeScript, con arquitectura por capas (UI, servicios API, tipos).
- La integracion de datos depende de una API externa y ya se contempla normalizacion defensiva de payloads.
- Se detecto deuda tecnica de tipado (uso de any) reflejada en resultados de lint.

## Completado
- Definicion del problema de negocio y prioridades de fase 1.
- Identificacion de stakeholders y criterios de exito inicial.
- Documentacion del stack y limites tecnicos reales del repositorio.
- Alineacion de principios tecnicos: integracion incremental, API-first y resiliencia de UI.
- Base de gobernanza del agente creada en AGENTS.md y .agents/.
- Primera automatizacion reusable del flujo de trabajo creada en skills/ para readiness pre-commit.
- Estructura frontend inicial creada en uis/ con dos apps Next.js + TypeScript (website y backoffice).
- Web corporativa de Hito 1 migrada a uis/website en componentes React reutilizables y ruta /.
- Backoffice inicial en uis/backoffice con layout propio y vista / conectada a la logica de negocio del Hito 2 sin copiar codigo.
- AUTH-01 implementado en services/api con JWT stateless, usuarios/perfiles en TinyDB, dependencia get_current_user y proteccion de rutas sensibles existentes.
- Nuevos modulos backend activos bajo /auth, /users y /profiles con validacion de credenciales hasheadas y expiracion de token por entorno.
- AUTH-03 implementado: flujo completo de restablecimiento y cambio de contrasena. Backend con /auth/forgot-password (siempre 200, anti-enumeracion), /auth/reset-password (token single-use via tabla password_resets, 400 en invalido/expirado/usado) y /auth/change-password (autenticado, verifica contrasena actual). Integracion de email transaccional con Resend. Frontend con /forgot-password, /reset-password (lee token del query string), /account/change-password y enlace de recuperacion en /login. Backend validado end-to-end via HTTP.
- Endurecimiento de seguridad: services/api/.env retirado del control de versiones y .gitignore actualizado (.env, .venv, __pycache__); secretos solo por variables de entorno.
- Auditoria completa del monorepo realizada y CLAUDE.md raiz creado con comandos, arquitectura y reglas operativas para sesiones de agente (complementa AGENTS.md, no lo sustituye).
- Bateria de pruebas unitarias implementada (AUTH-088 + extras API-042 y FE-019): 49 tests de pytest (auth completa con token expirado, reset single-use y anti-enumeracion; suppliers e incidents) con cobertura 81% total y >=70% en modulo auth, mas 13 tests de Jest (session.ts y logica de negocio de src/utils). TESTING.md documenta plan, resultados y hallazgos; bug real corregido: pyproject.toml no declaraba resend.
- Auditoria de gestion de errores completada y correcciones aplicadas en todas las capas: eliminados console.log con datos personales de candidatos, mensajes tecnicos crudos (Error HTTP / API_422_DETAIL) reemplazados por mensajes legibles generados centralmente, fetch y parseo JSON protegidos, botones de reintento en estados de error (dashboard tracker, edit page, directorio proveedores), errores 422 por campo via ApiFieldError en ambos registros, y scripts Python con stderr + sys.exit(1) (analyze, seed_incidents, seed de proveedores). Verificado con builds en verde y pruebas de casos de error por CLI.
- Gestor de incidencias centralizado implementado: modelo Incident en TinyDB, endpoints /api/incidents (crear, listar con filtros, detalle, cambio de estado con ciclo de vida, summary) protegidos con JWT, validacion compartida movida a packages/shared/incidents_validation, seed idempotente del CSV historico (94 registros validos con conteos verificados contra el CONTEXT) y UI en uis/backoffice (/incidents, /incidents/new con aviso de datos de pacientes, /incidents/summary). Backend validado end-to-end via HTTP (200/201/400/401/404 y transiciones invalidas) y build de Next.js en verde. Nota: las etiquetas de esta UI estaban en ingles por requisito original del CONTEXT del hito; se tradujeron al espanol el 2026-08-09 por decision explicita del usuario (ver entrada de rediseno mas abajo) — si se vuelve a evaluar contra ese CONTEXT especifico, tenerlo en cuenta.
- Gestor de inventario implementado (Hito 5, rama feat/inventory-orm-supabase): segunda base de datos (Supabase/PostgreSQL) via SQLModel conviviendo con TinyDB. Modelos MedicalSupply/SupplyDelivery/SupplyConsumption con FK a nivel de base de datos, endpoints bajo /inventory (products GET/POST, products/{id} GET, orders/inbound POST, orders/outbound POST, orders GET) protegidos con JWT existente, current_stock siempre calculado (nunca columna editable) y rechazo de stock negativo con 400 antes de escribir. seed_inventory.py carga los datos minimos del CONTEXT (6 supplies, 4 deliveries, 3 consumptions). 7 tests nuevos en tests/test_inventory.py (56 tests de pytest en total, 0 regresiones) usando SQLite en memoria por test en vez de Supabase real. Verificado tambien end-to-end contra Supabase real: creacion de tablas automatica al arrancar, stock calculado correcto tras seed, rechazo 400 por stock insuficiente y 422 por consumption_type invalido confirmados via curl.
- Interfaz de inventario en el backoffice implementada (Hito 5 frontend, rama feat/backoffice-inventory-ui sobre feat/inventory-orm-supabase): 4 pantallas bajo app/inventory/ (products, orders/inbound, orders/outbound, orders) mas services/inventoryApi.ts y types/inventory.ts. Stock con codigo de color por umbral (getStockLevel, umbrales documentados). El formulario de salida deriva el stock disponible del producto seleccionado desde la lista ya cargada (sin peticion extra) y bloquea el envio en cliente si la cantidad supera el stock; el 400 de la API por stock insuficiente se muestra igualmente junto al campo de cantidad como salvaguarda de servidor. Las 4 rutas quedan protegidas gratis por el AuthGuard global existente en layout.tsx. 6 tests nuevos en __tests__/inventoryStock.test.ts (19 tests de Jest en total, 0 regresiones). Probado de punta a punta en navegador real (Chrome via MCP) contra el backend y Supabase reales: alta de entrega, alta de consumo con actualizacion reactiva de stock, aviso de stock insuficiente en cliente, historial reflejando el user_uuid real de la sesion, y redireccion a /login al cerrar sesion.
- Rediseno visual completo de uis/backoffice (misma rama feat/backoffice-inventory-ui, 2026-08-09): tema oscuro "Supply Manifest console" (acento ambar, LEDs de estado, tipografia monoespaciada para datos) elegido por el usuario entre dos propuestas mostradas como artifact HTML comparativo. Incluye toggle real claro/oscuro (components/ThemeToggle.tsx, persistido en localStorage, sin flash gracias a un script inline en el head — con suppressHydrationWarning en <html> para el mismatch esperado) y traduccion completa de todo el backoffice al espanol, incluida la seccion de Incidents (decision explicita del usuario, se aparta de un requisito previo de ese hito especifico — ver nota arriba). Verificado build + 19 tests de Jest en verde, y recorrido visual completo en Chrome real (ambos temas, varias pantallas) sin errores de consola.
- Contenedorizacion completa del monorepo con Docker (rama feat/dockerize-monorepo sobre feat/backoffice-inventory-ui, ticket infra-40, 2026-08-10). src/ (utilidades TS del hito 2) se movio a packages/shared/business-logic/ para que el codigo compartido entre apps viva en un solo sitio, junto al paquete Python que ya existia ahi; convertirlo en npm workspace real se evaluo y quedo documentado en CLAUDE.md como mejora futura (mayor alcance/riesgo, requiere consolidar los package-lock.json independientes de cada app). Dockerfiles en uis/ (Node 20 Alpine, arranca website:3000 y backoffice:3001 desde un solo contenedor via start.sh) y services/api/ (Python 3.12-slim, uv, uvicorn --reload); docker-compose.yml en la raiz con red healthcore-net nombrada y BACKEND_API_URL=http://api:8000 (nombre de servicio, nunca localhost) para el rewrite proxy del backoffice.
  Verificado end-to-end con docker compose up real. Gotchas encontrados y resueltos (documentados en CLAUDE.md): Turbopack entra en panico sobre el bind mount virtiofs de Docker Desktop en Mac (fix: --webpack solo dentro de Docker, dev nativo sigue con Turbopack); webpack tampoco detecta cambios de forma fiable sobre virtiofs sin polling (fix: watchOptions.pollIntervalMs condicionado a DOCKER_DEV=true en next.config.ts de ambas apps); cache .next de una app puede quedar con la forma vieja de Turbopack tras cambiar a webpack e impedir el hot reload sin dar error visible (fix: borrar esa cache especifica); volumenes anonimos necesarios para node_modules (el bind mount de uis/ taparia los node_modules Linux instalados en la imagen); libpq5 y libc6-compat instalados por precaucion en los Dockerfiles (psycopg2-binary y el binario nativo de SWC respectivamente). Con todos los fixes: build limpio, hot reload confirmado en ambas apps via bind mount, y login del backoffice contra la API confirmando la comunicacion por nombre de servicio Docker.
- Optimizacion de rendimiento con caching (rama feat/caching-optimisation sobre feat/dockerize-monorepo, 2026-08-10): middleware de timing en main.py mas cache TTL en memoria (services/api/cache.py) para GET /inventory/products (30s) y GET /api/incidents/summary (60s), con invalidacion explicita en cada endpoint de escritura correspondiente. Medido en vivo contra Supabase real y 94 incidencias sembradas: /inventory/products paso de ~870ms (frio, N+1 contra Supabase) a ~2-4ms (cacheado); /api/incidents/summary de ~2.2ms a ~1.0ms (barrido x5 de la tabla evitado). Bug real encontrado y corregido antes de dar la feature por terminada: la cache es un singleton de proceso que sobrevivia entre tests de pytest aunque `clean_db` reseteara las bases de datos directamente sin pasar por los endpoints que invalidan — se anadio `cache.clear()` a esa fixture autouse. Frontend: Lazy Loading con next/dynamic en ProviderForm (/suppliers, detras de un toggle "+ Anadir proveedor") y OrderDetailPanel (/inventory/orders, detalle expandible con IDs de auditoria); useMemo para un resumen de gasto mensual por moneda en ProviderDirectory.tsx (justificado por los re-renders frecuentes que ya provoca la edicion de tarifas en la misma tabla). Se investigo y descarto aplicar next/dynamic a la portada del backoffice (Server Component) tras confirmar en la documentacion real de Next 16 que ahi no da code-splitting efectivo — ver detalle en techContext.md y en CACHING_REPORT.md (raiz del repo, informe completo del hito). 56 tests de pytest y 19 de Jest en verde, build de Next.js limpio.
- Plan de telemetria (rama feat/telemetry-design-plan sobre feat/caching-optimisation, 2026-08-12): entregable de diseno puro, sin codigo ni instrumentacion, en docs/telemetry/telemetry-plan.md + docs/telemetry/event-schemas.json. Catalogo de 23 eventos (5 obligatorios del CONTEXT del hito + 18 identificados en el plan) cubriendo inventario, incidencias, proveedores, autenticacion, rendimiento, errores de frontend y navegacion, con Event Envelope comun, allowlist de propiedades por evento y decision stream/batch justificada por urgencia de negocio (7 eventos en stream, 16 en batch). Encontradas y documentadas brechas reales entre el CONTEXT y el codigo de inventario vigente que bloqueaban instrumentar de verdad dos de los cinco eventos obligatorios — resueltas en la entrada de abajo.
- Instrumentacion de telemetria (rama feat/telemetry-capture sobre feat/telemetry-design-plan, 2026-08-14): implementacion real del plan de la entrada anterior. Backend: migracion minima (expiry_date en MedicalSupply, tabla SupplyThreshold, endpoint PATCH /inventory/products/{id}/stock que siempre rechaza) que desbloquea los 5 eventos obligatorios del CONTEXT; seed_inventory.py extendido con casos que disparan 2x stock_threshold_triggered y 1x supply_expiry_flagged; servicio compartido telemetry_service.py (log_event, hash_identifier HMAC) y stub POST /telemetry/events sin auth ni persistencia (Fase 1). Eventos conectados: los 5 obligatorios de inventario, mas login_succeeded/login_failed (backend, por el HMAC), session_expired, frontend_error_captured (window.onerror + unhandledrejection + app/error.tsx), page_viewed, api_latency_recorded, api_error_response y web_vital_recorded (evento nuevo, no estaba en el catalogo original — event-schemas.json paso de 23 a 24). Frontend: TelemetryService (uis/backoffice/services/telemetry.ts) con cola, batch 10s/20 eventos, sendBeacon en visibilitychange y reintento con backoff exponencial (3 intentos). 12 tests nuevos de pytest (74 en total, 0 regresiones) y un bug real encontrado y corregido en el camino: create_product usaba model_dump(mode="json"), que convertia expiry_date a string — SQLite (usado en tests) solo acepta objetos date reales para columnas Date, a diferencia de Postgres/Supabase que es mas permisivo. Decision deliberada de alcance: las categorias de producto (VALID_SUPPLY_CATEGORIES vs las del CONTEXT) no se migraron hoy — ver techContext.md.
- Almacenamiento real de telemetria (Hito Fase 3, rama feat/telemetry-storage sobre main actualizada, 2026-08-17): reemplazado el stub POST /telemetry/events por el endpoint real. Tabla nueva `telemetry_events` en Supabase (`services/api/telemetry_models.py`, registrada en el mismo `SQLModel.metadata` que inventario — se crea sola en el startup existente, sin SQL manual) con indices en `timestamp`, `event_type` y GIN sobre `tags`. Validacion por evento (`TelemetryEvent.model_validate` en un loop, nunca `list[TelemetryEvent]` tipado como body) para que un evento invalido no tumbe el lote con 422; insercion de los validos en una sola transaccion (`telemetry_repository.bulk_insert`). El modelo `TelemetryEvent` no se toco. 6 tests nuevos de pytest (80 en total, 0 regresiones).
  Bug real encontrado y corregido durante la verificacion manual contra Supabase real (no lo detectaban los tests con datos "limpios"): un evento con `timestamp` sintacticamente valido para Pydantic (`str` cualquiera) pero no parseable como fecha real (ej. "not-a-real-timestamp") pasaba la validacion de esquema y luego reventaba `build_event_record` con un 500 que tumbaba el lote completo — contradecia el principio de rechazo parcial. Se envolvio tambien la construccion del registro (no solo la validacion Pydantic) en manejo de errores por evento.
  Decisiones documentadas en `docs/telemetry/telemetry-plan.md` seccion 9: `tags` guarda `properties` mas los campos de correlacion del envelope (eventId/sessionId/userId/requestId/schemaVersion); `level` se deriva por palabras clave del `event_type` (`derive_level`); los eventos que el propio backend emite sin pasar por el endpoint HTTP (login_failed, api_error_response, stock_threshold_triggered, etc.) siguen sin persistirse — conectar eso habria hecho que tests de login/inventario intentaran hablar con Supabase real por un path que la fixture `client` no intercepta.
  Gotcha de entorno real (no de codigo): el `DATABASE_URL` del `.env` original apuntaba a un proyecto de Supabase inaccesible (tenant/user no encontrado), y despues la conexion directa nueva (`db.<ref>.supabase.co:5432`) fallo por DNS — Supabase solo publica un registro AAAA (IPv6) para conexiones directas desde 2024, sin add-on de IPv4. Solucion: usar siempre la cadena del **connection pooler** (`aws-0-<region>.pooler.supabase.com:6543`, usuario `postgres.<project-ref>`), que si resuelve en IPv4.
- Reporte tecnico + pipeline de analisis con Pandas (Fase 4 del brief, rama feat/telemetry-report sobre feat/telemetry-storage, 2026-08-21): `services/telemetry/analysis.py` con 4 funciones de metrica puras (volumen por dia/tipo, tasa de error por dia via la columna `level`, latencia via `web_vital_recorded`, y tasa de fallos de login como actividad adicional), todas cargando solo el rango de fechas necesario en SQL antes de agrupar con Pandas. Endpoint nuevo `GET /telemetry/report` (`routes/telemetry.py`) con cache de 60s reutilizando el `TTLCache` ya existente en `cache.py`. Decision explicita del usuario: se amplio el alcance para que `login_succeeded`/`login_failed` tambien se persistan en Supabase (antes solo se logueaban, ver punto 9 de "Proximos pasos" mas abajo, ahora resuelto parcialmente para estos dos eventos) — via una dependencia nueva `get_inventory_db_optional` que degrada a `None` en vez de romper el login si Supabase no responde. 30 tests nuevos de pytest, 0 regresiones (suite completa corrida en un entorno con iCloud Drive sincronizando el repo en vivo, lo que causo cuelgues intermitentes de varios minutos por descarga bajo demanda de archivos de `.venv`/`.git`, y en un momento dado la eviccion completa de la copia local del proyecto por espacio en disco bajo — resuelto moviendo el proyecto fuera de una carpeta sincronizada con iCloud; ver techContext.md).
  Verificacion final con datos 100% reales (no sinteticos) generados navegando el backoffice real en Chrome: registro de usuario, login fallido y exitoso, entregas/consumos de inventario, varias paginas — 25 filas en `telemetry_events`. En el camino se encontro y corrigio un bug real preexistente (PR aparte, `fix/telemetry-frontend-endpoint-resolution` sobre `feat/telemetry-storage`): `telemetry.ts` nunca enviaba nada al backend porque `ENDPOINT` se resolvia siempre a `""` (ver entrada detallada en techContext.md) — sin ese fix, ningun evento del frontend habria llegado jamas a Supabase pese a que la UI reportaba exito en cada accion. PR del reporte tecnico: #18. PR del fix: #19.
  Incidencia de ramas resuelta el 2026-09-06 (no fue un conflicto de codigo, fue orden de merges): los PRs #15/#16/#18/#19 se abrieron con base en otra rama feature en vez de `main` y se mergearon todos en ~90 segundos. El #17 (`feat/telemetry-storage` -> `main`) entro 23 segundos ANTES que el #18 (`feat/telemetry-report` -> `feat/telemetry-storage`), asi que la Fase 4 se quedo fuera de `main` sin que nada fallara visiblemente. Recuperada con el PR #21 (`feat/telemetry-report` -> `main`, merge limpio verificado con `git merge-tree`, 89 tests en verde). Tambien se cerro sin mergear el PR #20, creado por accidente con diff vacio contra `main`. Leccion operativa: abrir cada PR de hito con base `main`, no encadenarlos sobre la rama del hito anterior.
- Auditoria de rendimiento frontend completada sobre los dos frontends (rama `feat/frontend-performance-audit` sobre `main`, 2026-09-07), con el ciclo medir -> analizar -> corregir -> volver a medir y entregables `AUDIT.md` + `REPORT.md` + `audit/before|after/` (informes HTML de Lighthouse 13.4.1 y capturas). Hallazgo principal: `/application` de `uis/website` tenia un CLS de 0.974 (umbral 0.1) porque la pagina es un Client Component que declaraba sus estilos en `<style jsx>` de styled-jsx — en el App Router ese CSS lo inyecta JavaScript despues de la hidratacion, asi que el HTML del servidor llegaba sin una sola regla para `.application-card` y la seccion de 1286 px se recolocaba entera al hidratar. Movidos a `app/application/application.css` (chunk propio de la ruta, no globals.css): CLS 0.974 -> 0, Performance 76 -> 99. Resto de correcciones: imagen del LCP del hero con `loading="eager"` + `fetchPriority="high"` (element render delay 792 ms -> 22 ms), `next/image` con `width`/`height`/`sizes` en las 4 imagenes usando un loader propio hacia Unsplash en vez del optimizador integrado, fuente Manrope conectada por fin via `next/font` (`globals.css` pedia `var(--font-manrope)` y nadie la definia desde la migracion a React), y `app/icon.svg` en ambas apps (Best Practices 96 -> 100 en las cinco mediciones). Refactorizacion extraida: `hooks/useAsyncData.ts` + `components/AsyncSection.tsx` en el backoffice, adoptados por las 4 pantallas que copiaban el patron (-65 lineas), corrigiendo de paso una condicion de carrera real en `/incidents`. Bateria de tests: 30/30 (19 previos + 11 nuevos, sin añadir dependencias).
- Auditoria de serializacion del backend completada (rama `feat/serialization-audit` sobre `main`, 2026-09-07). Los 37 endpoints de `services/api` tienen ahora contrato de respuesta explicito, documentado en `docs/serialization-audit.md` con su estado original, la clasificacion en tres estados y la decision razonada de cada uno. Punto de partida mejor de lo que asumia el brief: 24 endpoints ya estaban correctos y ningun esquema del proyecto ha contenido nunca `hashed_password`. Los 6 sin serializar devolvian dicts sin tipar (`/api/health`, `/api/incidents/analyze`, `/telemetry/report`) o no declaraban su content-type (`/`, export CSV, PATCH de stock). Los 7 parciales eran el problema real: `POST /users` (registro, flujo NO autenticado) devolvia email y perfil completo; `GET /users` devolvia el email de TODAS las cuentas a CUALQUIER usuario autenticado; `GET /suppliers` enviaba 12 campos por fila y la tabla usa 8 (medido: 373 -> 202 B/fila, 45% menos). Esquemas nuevos: UserRegistered, UserListItem, ProfilePublic, SupplierListItem, IncidentListItem, HealthStatus, IncidentAnalysisResponse, DirectStockEditRejection y TelemetryReport con sus 6 filas. Decision de alcance adicional: `GET /users` pasa a exigir `require_admin` (la serializacion arregla QUE se devuelve, no A QUIEN). `user_uuid` se mantiene en inventario a proposito, documentado: es el rastro de auditoria HIPAA, la tabla lo muestra y no hay objeto usuario anidado que ofrecer (usuarios en TinyDB, ordenes en Supabase). Tests: 101 en verde (89 previos + 12 nuevos en `tests/test_serialization.py`, tres de ellos barridos sobre todo el esquema OpenAPI para que un endpoint futuro que filtre credenciales falle solo). Verificacion manual de 7 endpoints contra la API viva sobre base TinyDB temporal. Tipos de TypeScript del backoffice actualizados (IncidentListItem, SupplierListItem, AuthProfile sin ids); `tsc --noEmit` localizo los dos puntos que asumian la proyeccion completa.
  Consolidacion en `main` el 2026-09-12: PRs #22 (incidencia de ramas), #23 (rendimiento frontend) y #24 (serializacion) mergeados en ese orden, y ramas feature antiguas ya integradas borradas del remoto. Las tres eran mergeables por separado pero chocaban entre si al entrar la primera (todas anadian entrada en este archivo). Unico conflicto de codigo real: `uis/backoffice/app/incidents/page.tsx`, donde el refactor `useAsyncData`/`AsyncSection` de #23 y el tipo `IncidentListItem` de #24 se combinaron a mano — git no marcaba los dos `Incident[]` que traia #23, que habrian roto el typecheck. Validado tras el merge: pytest 101/101, jest 30/30, `tsc --noEmit` limpio (requirio `npm ci` en `uis/backoffice`, que no tenia dependencias instaladas en local).
- Dashboard tecnico de telemetria en el backoffice (rama `feat/telemetry-dashboard` sobre `main`, 2026-09-12). Actividad adicional "Dashboard visual simple" del brief del reporte tecnico, que se volvio a asignar como clase: el resto del brief (analysis.py, GET /telemetry/report con cache 60s, auth_failure_rate) ya estaba entregado desde el 2026-08-21, verificado en codigo antes de empezar. Pantalla `/telemetry` con un panel por metrica (pregunta operacional, barras CSS sin libreria de graficos por decision del usuario, tabla de detalle), periodo devuelto por el servidor y selector de fechas (decision del usuario de ampliar el alcance). Conversion dias-incluidos -> fin exclusivo UTC en `toReportQuery`, fijada con tests en frontend y backend. Gotcha encontrado por los tests: `min`/`max` en inputs de fecha activan la validacion nativa y bloquean el submit, por eso el formulario lleva `noValidate`. Validado: jest 53/53 (23 nuevos), pytest 109/109 (1 nuevo de contrato + 3 de cache + 4 de logs de email), `tsc --noEmit` limpio y `next build` compila `/telemetry`. Verificado contra la base real (29 eventos): el rango que envia la pantalla para "del 17 al 21 de agosto" devuelve las 4 metricas completas, incluido el dia 21 entero. Encontrado en esa verificacion y **corregido en la misma rama**: la cache de `GET /telemetry/report` (requisito obligatorio de la Fase 2, entregada en agosto) no funcionaba sin parametros — la clave incluia `datetime.now()` con microsegundos y cada peticion sin fechas recalculaba (~370 ms en cada una de 3 seguidas). Ahora la clave sale de los parametros pedidos (`_report_cache_key`); medido contra Supabase real: ~460 ms la primera, ~4 ms las siguientes. 3 tests nuevos; el de la ventana por defecto falla con el codigo antiguo. Recorrido en Chrome real con sesion del usuario: rango por defecto con los eventos del propio dia (agrupados por fecha UTC), rango 17-21 de agosto con los 4 paneles identicos a la respuesta de la API, validacion de rango invertido sin peticion, modo claro, y consola sin errores. Mejoras visuales detectadas en ese recorrido y aplicadas: `title` en las etiquetas de barra (los `event_type` largos se cortaban sin forma de leerlos, fijado en el test de la pantalla) y rejilla `minmax(min(100%, 420px), 1fr)`, que da 2x2 en escritorio en vez de 3+1 y una columna en movil.
  Mismo dia, fix aparte de la verificacion manual del reset de contraseña: `RESEND_API_KEY` tenia el valor de ejemplo de `.env.example` (el `.env` se rehizo al cambiar `DATABASE_URL`) y `/auth/forgot-password` respondia 200 sin enviar nada, como debe por anti-enumeracion — la causa solo estaba en el log del servidor. Ese log escribia el email del destinatario en texto plano, contra la regla de no poner datos de usuario en logs: `email_service.py` ahora loguea tipo + motivo del error con cualquier direccion enmascarada (`_redact_emails`) y sin `logger.exception`, cuyo traceback repetiria el mensaje crudo del proveedor. 4 tests en `tests/test_email_service.py` (3 fallan con el log anterior). Correo de reset verificado de punta a punta tras poner una clave real.
- Diseño del pipeline de desempeño de negocio, Parte 1 de 3 del hito de Data Pipelines (rama `feat/business-pipeline-design`, **apilada sobre `feat/telemetry-dashboard`** por decisión del usuario: las ramas de clase se apilan y se mergean todas juntas cuando el profesor apruebe, 2026-09-13). Entregable: `data/pipelines/PIPELINE_DESIGN.md`, sin código de orquestación. Pipeline `monthly_clinic_supply_performance` para el "Reporte Mensual de Desempeño de Insumos por Clínica" (CEO y CCO): estado actual y brecha frente al reporte técnico, extracción SQL por ventana mensual UTC sobre `telemetry_events` en solo lectura, diagrama Mermaid extraer → transformar → validar → cargar, reglas por KPI, upsert sobre `unique (clinic_id, month_start)` en una única transacción, tablas `reporting.monthly_clinic_supply_performance` (exacta del CONTEXT), `reporting.pipeline_runs` (18 campos justificados, lock por ventana con índice único parcial) y `reporting.pipeline_run_partitions` (valores antes/después), mapeo a Prefect (1 flow, 7 tasks, estados, blocks `Secret` y `JSON`, deployments del día 1 y del día 8), 3 endpoints de `services/reporting/` con su función de `data/pipelines/`, y respuestas a las 10 preguntas del brief. Cambio aditivo de captura: `unit_cost` opcional en `inbound_order_created` (`docs/telemetry/event-schemas.json`). Decisiones del usuario: `clinic_id` como id real en texto, sin catálogo inventado; `unit_cost` solo en el esquema hoy. Decisión delegada por el usuario: `critical_stockout_count` cuenta insumos distintos en quiebre, no eventos crudos (el conteo literal se conserva en `source_event_counts`). Validado: pytest 109/109 y jest 53/53, sin regresiones (cambios solo de documentación y esquema).
- Pipeline de desempeño de negocio implementado, Parte 2 de 3 (rama `feat/resilient-business-pipeline`, apilada sobre `feat/business-pipeline-design`, 2026-09-13). Prefect `>=3.4,<4` (3.4.25 en el venv 3.9 local, instalado con pip porque `uv` no está en la máquina).
  - **Pipeline:** `data/pipelines/pipeline.py` define el flow `monthly_clinic_supply_performance_flow` con 8 tasks. Hay reintentos justificados en todas las que tocan Supabase, dos tasks no críticas con `return_state=True` (`extract_domain_activity` y `export_eval_snapshot`, que escribe en `data/eval/`), caché de 1 h en la transformación con clave por huella del contenido extraído, y un CLI con `--month-start`. Soporte en `data/pipelines/monthly_clinic_supply_performance/` (`models.py`, `storage.py`, `run_log.py`, `queries.py`, `trigger.py`) y `data/process/supply_performance_transforms.py`.
  - **Carga:** idempotente con `ON CONFLICT (clinic_id, month_start)` en una transacción, más `reporting.pipeline_run_partitions`. Log de corridas en `reporting.pipeline_runs`, con lock por mes (índice único parcial) y heartbeat de 30 min.
  - **Endpoints** en `services/reporting/`: KPIs, última corrida y disparo manual (solo admin, 202/409/400), que ejecuta el flow real en `BackgroundTasks`.
  - **Requisitos previos P1-P3 resueltos:** campo "Coste unitario" en el formulario de entregas con `unit_cost` en el evento; `stock_threshold_triggered` persistido y emitido solo al cruzar el umbral; `supply_expiry_flagged` por lote y clínica, persistido y idempotente (`inventory_alerts.py`, `eventId` uuid5).
  - **Decisiones del usuario:** incluir P1-P3 en esta clase; KPIs con la definición literal del CONTEXT, arreglando el origen del aviso de stock en lugar de contar "insumos distintos" (el brief prohíbe reinterpretar KPIs); disparo manual dentro de la API sin servidor ni worker de Prefect.
  - **Verificado contra Supabase real:** la primera ejecución falló por un bug real (timestamps con y sin microsegundos; Pandas 2 infería el formato de la primera fila), quedó registrada como `failed` y se corrigió con `format="ISO8601"` y un test que falla sin el arreglo. Después, dos ejecuciones de agosto: 2 filas `inserted` y luego 2 `unchanged`, con la transformación `Cached`. Estado `completed_with_warnings` por baja cobertura real (inventario de agosto sin eventos, anterior al fix del frontend del 2026-08-21). `telemetry_events` intacta (70 filas); tabla de KPIs creada con el DDL literal del CONTEXT. La API arranca contra Supabase sin errores y la búsqueda JSONB de `inventory_alerts` funciona en Postgres.
  - **Tests:** pytest 134/134 (109 previos + 22 del pipeline con `prefect_test_harness` + 3 de P2/P3); jest 60/60 (53 + 7 de `parseUnitCost`); `tsc --noEmit` limpio y `next build` compila.
- Pipeline de negocio, Parte 3 de 3: subflows, tests unitarios y dashboard (rama `feat/pipeline-subflows-dashboard`, apilada sobre `feat/resilient-business-pipeline`, 2026-09-13).
  - **Subflows:** el flow principal de `data/pipelines/pipeline.py` ya no contiene lógica de ETL; coordina `extract_clinic_supply_activity` → `compute_monthly_clinic_supply_kpis` → `load_monthly_clinic_supply_performance` → `export_supply_performance_eval_snapshot` (opcional, `return_state=True`), cada uno con entradas y salidas explícitas y ejecutable por separado. Una task por KPI con el nombre del CONTEXT, sobre funciones puras y defensivas en `data/process/supply_performance_transforms.py`.
  - **Minimización adelantada:** la extracción recorta `tags` antes de devolverlos, porque Prefect guarda los parámetros de cada subflow en su base.
  - **Tests unitarios** en `tests/pipelines/test_pipeline.py` (10, `task.fn` sin base de datos ni Prefect; KPI calculado a mano y casos defensivos). Comprobado que protegen: con la fórmula del coste saboteada fallan 3.
  - **Dashboard `/reporting`** en el backoffice ("Informe mensual"), de solo lectura por decisión del usuario: un panel por KPI con el nombre exacto del CONTEXT, período, costo por país y moneda, conteos con total de red.
  - **Actividad adicional elegida por el usuario:** avisos en lenguaje de negocio de informe atrasado (`is_stale`) y captura incompleta (preguntas de diseño 4 y 6). Descartado el índice de `eventId`, porque exige tocar `telemetry_events`.
  - **Cambios de soporte:** `http.ts` lanza `ApiRequestError` con `status` (compatible con las pantallas existentes); las barras CSS se extrajeron a `components/BarList.tsx`.
  - **Bugs reales encontrados por los tests:** `get_run_logger()` impedía llamar las tasks fuera de Prefect (resuelto con `_logger()`); el selector de mes del dashboard no se podía vaciar (efecto con dependencia de más).
  - **Verificado:** pytest API 136/136, `tests/pipelines` 10/10, jest 84/84, `tsc` limpio, `next build` con `/reporting`. `python data/pipelines/pipeline.py` contra Supabase tras el refactor dio `unchanged: 2` (mismos KPIs que la Parte 2) y `Cached` en la segunda corrida. Recorrido visual en Chrome con la sesión del usuario contra Supabase real: 4 paneles con datos de agosto, avisos, modo claro, mes sin informe y consola sin errores. Ese recorrido destapó que el costo salía `0,00` sin explicación para compras sin `unit_cost`. Corregido: `clinics_with_unrecorded_cost` en `GET /reporting/pipeline-runs/latest`, marca "Incompleto" y aviso en el dashboard. Tests finales: API 136/136, `tests/pipelines` 10/10, jest 85/85.
- Job nocturno de telemetría, Ticket #DEV-53 "Procesos en Segundo Plano" (rama `feat/nightly-telemetry-export`, apilada sobre `feat/pipeline-subflows-dashboard`, 2026-09-13). Detalle y logs reales en `docs/nightly-export.md`.
  - **Qué hace:** `scripts/nightly_export.py` exporta `telemetry_events` del día anterior (UTC, o `TARGET_DATE`) a `data/raw/telemetry_YYYY-MM-DD.csv` si no existe, lanza `data/pipelines/pipeline.py` como subproceso y registra la ejecución en la tabla nueva `job_runs` (`pending → processing → completed | failed`). La lógica de estado vive en `services/job_runner/` y la migración SQL en `services/job_runner/migrations/001_create_job_runs.sql`, que el script ejecuta al arrancar.
  - **Decisiones del usuario:** el pipeline recalcula cada noche el último mes cerrado (sin argumentos); el disparador es un contenedor `scheduler` dedicado en `docker-compose.yml` (supercronic, `15 2 * * *` UTC); se incluye rescate de zombis por antigüedad (3 h) y SIGTERM → `failed`. Delegada por el usuario: la fila que pierde la carrera por el lock queda `failed` con prefijo `Cancelada (sin trabajo):`, no borrada.
  - **Lock:** el estado `processing`, hecho atómico con un índice único parcial. Se comprobó que hace falta: con dos procesos reales, las dos instancias pasaron la consulta previa en el mismo segundo.
  - **Verificado contra Supabase real:** 24 filas exportadas del 21 de agosto y pipeline con código 0 (`unchanged: 2`); repetición `skipped`; dos instancias a la vez → una `completed` y otra `cancelled`; SIGTERM a mitad → `failed` con `JobTerminatedError`, sin filas en `processing` ni corridas activas en `pipeline_runs`. En el contenedor (Python 3.12): crontab válido con `supercronic -test`, un día `completed` y otro `skipped` (el completado desde el Mac).
  - **Tests:** `tests/jobs` 33/33 (raíz, SQLite, sin FastAPI; incluye un test con dos procesos reales). Comprobado que protegen: con el estado por defecto saboteado a `completed` fallan 5, y sin el índice único fallan 3. Sin regresiones: API 136/136, `tests/pipelines` 10/10. Sin cambios de frontend.
  - **Encontrado y corregido con autorización explícita del usuario:** el `.env` de la raíz, que usa `docker compose`, apuntaba al proyecto antiguo de Supabase `eu-central-1`. Solo se sustituyó `DATABASE_URL` por la de `services/api/.env`. Verificado con el contenedor sin variables pasadas a mano (`skipped` para un día completado solo en la base buena). El scheduler no se ha dejado levantado.
- Cola de tareas asíncronas con Redis y Celery, Ticket #DEV-55 (rama `feat/async-task-queue`, apilada sobre `feat/nightly-telemetry-export`, 2026-09-13). Detalle, mediciones y borrador de PR en `docs/async-tasks.md`.
  - **Qué hace:** `POST /reporting/pipeline-runs` ya no ejecuta el pipeline con `BackgroundTasks` dentro de FastAPI. Encola la tarea de Celery `reporting.run_monthly_clinic_supply_performance` en Redis y responde 202 con `task_id`; la ejecuta un worker en otro proceso. `GET /tasks/{task_id}` (solo admin) devuelve `pending`/`started`/`success`/`failure` con resultado o error. Redis (`noeviction`), worker y Flower (5555) añadidos a `docker-compose.yml`.
  - **Decisiones del usuario:** endpoint del recálculo del informe (el más pesado: 3-11 s); rama apilada; `max_retries=3` literal (4 ejecuciones); tras medir que crear el lock en Supabase costaba ~520 ms, "la API comprueba y el worker bloquea". Delegada ("hazlo tú"): la traducción de estados de Celery (`RETRY`→`started`, `REVOKED`/`REJECTED`/`IGNORED`→`failure`, desconocido→`pending`).
  - **Reintentos y DLQ:** backoff 10/20/40 s sin jitter; tras el cuarto fallo, fila en `task_dead_letters` (Supabase) con `task_id`, intento, error y fecha. `SoftTimeLimitExceeded` va directo a la DLQ. Cada intento crea su propia fila en `reporting.pipeline_runs`.
  - **Bugs y problemas reales encontrados:** (1) el logger interno de Celery filtraba correos en los mensajes de reintento y tracebacks (corregido con un filtro de enmascarado); (2) Prefect lanza `CancelledRun` en vez de devolver `Cancelled`, y autoretry mandaba "mes ocupado" a la DLQ (lo detectó un test); (3) el contenedor `api` no montaba `services/` ni `data/`, así que `/reporting` no se podía importar en Docker desde la Parte 2 del pipeline; (4) 202 de ~500 ms, resuelto con la reserva + lectura `AUTOCOMMIT`.
  - **Verificado contra Supabase real** (Redis en Docker; API, worker y Flower nativos): 202 en ~120 ms en caliente (447 ms la primera tras un rato sin tráfico); ciclo `started→success` con los mismos KPIs de agosto; 409 con el `run_id` creado por el worker; mensaje en cola con la API apagada procesado al arrancar el worker; worker con Prefect inaccesible → reintentos a los 14/35/77 s, `failure` y fila en la DLQ. Capturas de Flower y log de reintentos en `docs/async-tasks/`. Limpieza: 11 corridas de verificación borradas, agosto sigue como última corrida, la fila de la DLQ se conserva como evidencia, Docker vacío y cerrado.
  - **No verificado:** construir la imagen del worker y arrancar `worker`/`flower` dentro de Docker (quedaban 2,8 GB de disco); `docker compose config` sí resuelve.
  - **Tests:** API 167/167 (136 + 31), cobertura de `services/tasks` 94-100 %; `tests/jobs` + `tests/pipelines` 43/43; jest 85/85 y `tsc` limpio (sin cambios de frontend).
- Modelo de predicción de ventas con regresión (rama `feature/sales-forecast-model`, apilada sobre `feat/async-task-queue`, 2026-09-14). Detalle en `docs/sales-forecast.md`.
  - **Qué hace:** `scripts/train_sales_forecast.py` carga `data/raw/healthcore_sales.csv` (fila `consolidated`, copia exacta del provisto), trata nulos, valida el CONTEXT, separa 2016-2023 / 2024-2025, entrena tendencia log-lineal + Random Forest estacional y reporta MSE, PSI, Gini y K2 Score (R²) sobre la prueba, con un gráfico que muestra la franja del 80 %. Lógica en `data/process/sales_forecast.py`.
  - **Decisiones del usuario:** Random Forest (explicabilidad, 96 filas, franja OOB sin datos extra), separar la tendencia porque los árboles no extrapolan, e instalar `uv` (con `uv pip install` en el venv de la API y dependencias en `requirements-ml.txt`). Asumido y documentado: "K2 Score" = R², y PSI de mezcla sobre `avg_revenue_per_visit_usd`, porque el CSV no trae filas por país.
  - **Resultados (random_state=42):** RMSE 94.777 USD = 2,82 % del ingreso mensual medio, R² 0,929, Gini 0,936, PSI de predicción 0,09 (estable), franja 80 % con cobertura 20/24. Crecimiento aprendido 4,16 % anual (CONTEXT: ~4 %).
  - **Hallazgos:** `revenue = visits × avg` exactamente, así que las visitas quedan fuera como variable (fuga de datos). La dispersión entre árboles daba una franja optimista (4/24 meses) y se sustituyó por errores OOB. PSI de mezcla 0,48: el ingreso por consulta sube un 1,3 % en 2024-2025; queda documentado para que lo confirme Revenue Cycle.
  - **Tests:** `tests/pipelines/test_sales_forecast.py` 15/15. Con un split aleatorio saboteado fallan 4. Sin regresiones: `tests/pipelines` + `tests/jobs` 58/58 y API 167/167 tras instalar las dependencias nuevas. Python no tiene linter configurado en el repo. Sin cambios de frontend.
- Evaluacion tecnica del modelo de ventas (rama `feature/regression-model-eval`, apilada sobre `feature/sales-forecast-model`, 2026-09-14). Reporte en `data/eval/evaluation_report.md`.
  - **Que hace:** `scripts/evaluate_sales_forecast.py` evalua, sin modificarlo, el modelo de la clase anterior y solo sobre 2016-2023. Incluye validacion cruzada temporal (`TimeSeriesSplit`, 5 pliegues de 12 meses) con MAE y RMSE de entrenamiento y validacion como media ± desviacion, y una curva de aprendizaje con ventanas moviles de 24 a 84 meses (`data/eval/learning_curve.png`). Tambien una referencia ingenua y el diagnostico `diagnose_fit`. Logica en `data/process/sales_forecast_evaluation.py`; cifras en `data/eval/sales_forecast_evaluation.json`.
  - **Decisiones:** metrica principal RMSE (decision del usuario), justificada por los meses atipicos que preocupan a Sandra y por la continuidad con el MSE del CONTEXT. Delegada por el usuario ("hazlo tu"): la regla de diagnostico, que compara el entrenamiento con la referencia para underfitting y usa brecha ≥ 1,5× o validacion ≥ referencia para overfitting.
  - **Resultado:** bien ajustado. RMSE 2,37 ± 0,17 % en entrenamiento y 3,22 ± 0,58 % en validacion (cociente 1,36), frente a 5,70 % de la referencia; las curvas convergen cerca del suelo de ruido.
  - **Hallazgo y accion correctiva propuesta (no implementada):** los anos de crecimiento del 2 % (2020, 2022) concentran un sesgo de +2,3-2,7 %, que supone el 45-47 % de su error. Anadir `year_parity` a la tendencia bajo la CV a 2,98 ± 0,45 % en una prueba exploratoria, pero introduce un sesgo de ~+1 % a investigar. Antes de aplicarlo, Revenue Cycle debe confirmar que la alternancia es estructural.
  - **Tests:** `tests/pipelines/test_sales_forecast_evaluation.py` 19/19. Con los pliegues saboteados (KFold barajado y sin verificacion) fallan los 2 tests de orden. Sin regresiones: `tests/pipelines` + `tests/jobs` 77/77 y API 167/167. Sin dependencias nuevas ni cambios de frontend.
  - **Anadido a peticion del usuario (commit aparte, mismo PR #32):** `data/eval/fit_diagnosis_map.png`, con el mapa de las zonas y las curvas de nuestro modelo frente a dos modelos malos a proposito (`data/process/sales_forecast_comparison.py`). "Solo tendencia" da 10,29 → 10,47 % (underfitting) y "Bosque que memoriza" 1,35 → 5,61 % (overfitting). Las funciones de evaluacion aceptan ahora un `Forecaster` y las cifras del modelo real no cambian (comparado con el JSON del commit anterior). Nueva seccion en el reporte. Tests 21/21, uno de ellos fija los tres veredictos.

- Pila de clases mergeada en `main` (2026-09-21, por orden del usuario tras la aprobacion del profesor): PRs #25-#32 (telemetry-dashboard → regression-model-eval), de abajo arriba y con merge commits, verificando que `main` quedaba identico a la punta de la pila. Ramas borradas en local y remoto. La siguiente clase ya sale de `main`.
- Base de conocimiento RAG, Hito 7 (rama `feature/rag-knowledge-base` sobre `main`, 2026-09-21). Diseno en `docs/rag/rag-design.md`.
  - **Que hace:** asistente para los coordinadores de pacientes. `setup()` trocea los 4 documentos del CONTEXT (`docs/company-knowledge-base/`) en 14 chunks semanticos y los indexa en Qdrant (`healthcore_knowledge`). `retrieve()` + `generate_answer()` = `query()`, expuesta en `POST /knowledge/query` (con autenticacion) y en la pantalla "Asistente" (`/knowledge`) del backoffice.
  - **Decisiones del usuario:** credenciales del proxy LLM de 4Geeks; Qdrant en Docker local, con limpieza de lo residual al terminar; endpoint y pantalla solo para usuarios logueados. Delegadas ("hazlo tu"): las reglas de negocio del prompt y la eleccion de los modelos (embeddings `pplx-embed-v1-0.6b`, el unico disponible; generacion `gpt-5.6-luna`).
  - **Resultado real:** Recall@3 del 100 % con `min_score=0.38` (objetivo 80 %), las 14 preguntas con el chunk correcto en 1.a posicion y ninguna de las 4 fuera de tema sobre el umbral. Respuestas reales correctas en las tres reglas del CONTEXT (seguro no listado → facturacion; separar EE. UU./R. U.; sin cargo por no-show a Medicare/Medicaid).
  - **Encontrado en Chrome, no en tests:** el modelo respondia en Markdown y la pantalla mostraba los asteriscos. Corregido en el prompt y con `toPlainText`.
  - **Tests:** `tests/pipelines/test_rag.py` 21, `services/api/tests/test_knowledge.py` 5 y `__tests__/knowledgeAssistant.test.tsx` 9. Sin el filtro de `min_score` fallan los 2 tests del umbral (comprobado). Sin regresiones: API 172, raiz (pipelines + jobs) 100, Jest 94, `next build` correcto.

- Agente de soporte con LangGraph, Parte 1 (rama `feature/langgraph-agent-base`, apilada sobre `feature/rag-knowledge-base` porque el PR del Hito 7 espera aprobacion, 2026-09-24). Diseno en `docs/agent/agent-design.md`.
  - **Que hace:** el RAG del Hito 7 como grafo LangGraph de 5 nodos (`receive_question`, `reject_question`, `retrieve`, `no_information`, `generate`) con dos aristas condicionales (pregunta vacia; sin contexto sobre el umbral), compilado y validado al arrancar la API, con checkpoint por transicion y trace JSON por corrida. Expuesto en `POST /agent/query` (convive con `/knowledge/query`).
  - **Decisiones del usuario:** trace en JSON propio (no LangSmith); la pregunta solo como huella SHA-256 + longitud; endpoint nuevo sin tocar la UI. Delegada ("hazlo tu"): `route_after_retrieve` no vuelve a mirar puntuaciones, el umbral vive solo en `retrieve()`.
  - **Resultado real:** 5 corridas reales grabadas, 26/26 evals (anclaje: "50 USD", Medicare sin cargo + historial, facturacion/Tom Callahan; fuera de tema sin llamar al modelo). El trace atribuyo 62,5 de 66 s a `generate`: la latencia rara del Hito 7 es del proveedor.
  - **Tests:** grafo 17, evals 26, endpoint 6. Sin regresiones: raiz (pipelines + jobs) 143, API 178. Sin cambios de frontend. Qdrant reindexado para grabar y retirado despues (contenedor, volumen e imagen).

- Agente de soporte con LangGraph, Parte 2 (rama `feature/langgraph-external-tools`, apilada sobre `feature/langgraph-agent-base`, 2026-09-24). Diseno en `docs/agent/agent-tools.md`.
  - **Que hace:** tools de solo lectura para incidencias (`lookup_incident`, 3 s) e inventario (`check_inventory_stock`, 5 s), en proceso sobre los repositorios reales, y un nodo `plan_sources` donde el modelo decide (function calling) si usar RAG, tools o ambos. Fallback honesto fijo si la tool falla, agota el timeout o el recurso no existe. Trace v2 con `sources_used`.
  - **Decisiones del usuario:** el modelo elige las fuentes; transporte en proceso; tool extra de inventario; evals sobre una TinyDB temporal sembrada con el seed oficial. Delegada ("hazlo tu"): cortocircuito a fallback en cuanto una tool falla.
  - **Bug real encontrado grabando:** con una sola funcion `lookup_incident` de campos opcionales, el modelo rellenaba todos los filtros con valores inventados y el contrato rechazaba la llamada (el agente caia al RAG). Corregido separando `get_incident`/`search_incidents` con filtros nullable; tests de regresion.
  - **Resultado real:** 12 casos grabados, 86/86 evals (tool sin RAG, RAG sin tools, mixto en orden, ticket inexistente, caida con TimeoutError a los 3,0 s, stock real de 350 cajas desde Supabase e insumo inexistente).
  - **Tests:** tools 39, grafo 17, evals 86, endpoint 8. Sin regresiones: raiz 242, API 180. Sin cambios de frontend.
- Supabase unificado (2026-09-24, a peticion del usuario, fuera del PR): todo HealthCore vive ahora en `healthcore-data` (antes `healthcore-inventory`, eu-central-1, ref `klaxalwsyrucwszvdkhu`). Se copio entera la base de eu-west-1 (`database-auditory`, ref `eizprmptspxhxvaqvqdz`, ahora pausada) desde su backup descargado; las tablas antiguas de eu-central-1 (seed duplicado del Hito 5 + 12 eventos de prueba) quedan archivadas en el esquema `archive_hito5`, sin borrar. Una transaccion, ensayo previo con ROLLBACK, verificacion de filas y stock. EduTrack no se migro (no es HealthCore).

- Servidor MCP con OAuth (rama `feature/mcp-oauth-tools` sobre `main`, 2026-09-25). Diseno en `docs/mcp/mcp-server.md`.
  - **Que hace:** `mcps/healthcore` expone por Streamable HTTP 5 tools (`incidents_get`, `incidents_search`, `incidents_create`, `incidents_update_status`, `inventory_query`) contra la API real, protegidas con MCP Auth (401 sin token valido, Protected Resource Metadata) y un scope por tool. El inventario es solo lectura en tres capas y rechaza escrituras con `read_only_resource`. Log de auditoria por invocacion. El agente consulta las incidencias como cliente MCP (`langchain-mcp-adapters`, token `client_credentials` con solo `incidents:read`); el acceso en proceso se elimino.
  - **Decisiones del usuario:** recrear el venv en Python 3.12; Logto Cloud como proveedor OAuth; guia escrita para que el usuario haga la prueba de MCP Playground en Codespaces. Delegada ("hazlo tu"): la tabla de scopes por tool.
  - **Encontrado al implementar:** mcpauth 0.1.1 no tiene modo resource server (se usa 0.2.0b1); el paquete `fastmcp` nuevo es incompatible con el adaptador (se usa el FastMCP del SDK); FastMCP antepone "Error executing tool" a los errores; la proteccion DNS-rebinding bloquearia Codespaces sin configurar hosts.
  - **Tests:** `test_mcp_server.py` 44 (cadena en memoria con emisor RSA local) y `test_agent_mcp_client.py` 5. Sin regresiones: API 224, raiz 247. Sin cambios de frontend.
  - **Prueba real con Logto (tenant `eb77o9`):** 401 sin token, ciclo de ticket, `read_only_resource`, `insufficient_scope` con el token del agente, inventario de Supabase y el agente real por MCP (`via=mcp`). Encontro un bug que los tests no veian (`httpx.Request(auth=...)` en el flujo OAuth del agente), corregido y con tests.
  - **Agente real:** 4 preguntas por `/agent/query` con el modelo (tras regenerar la `LLM_API_KEY` caducada), todas `via=mcp`; "cierra el ticket 22" no modifica nada (solo `incidents:read`).
  - **Pendiente:** la prueba de MCP Playground en Codespaces (la hace el usuario, guia en la §8 del diseno).

## En curso
- Consolidacion de modelo canonico de datos para candidatos (evitar divergencias stage/step y campos alternos).
- Mejora de robustez de tipos en frontend.
- Preparacion para observabilidad y trazabilidad orientadas a cumplimiento.
- Estandarizacion del flujo operativo del agente mediante skills reutilizables para memoria y validacion.
- Evolucion de UI interna del backoffice desde vista base hacia dashboard operativo por modulo.
- Endurecimiento del flujo de validacion manual en /docs para registro, login, autorizacion Bearer y pruebas de 401/403.

## Proximos pasos previstos (prioridad)
1. Resolver errores de lint relacionados con any y migrar a unknown + narrowing.
2. Estandarizar contrato de datos de Candidate y notas en una sola forma canonica.
3. Definir validaciones de entrada/salida en la capa de servicios API.
4. Establecer primera version de metricas operativas (no-shows, latencia de reporte, calidad de datos).
5. Disenar propuesta de BFF o backend propio para reducir dependencia de API externa y mejorar seguridad/auditoria.
6. Consolidar catalogo minimo de skills del agente para tareas repetitivas del monorepo.
7. Completar hardening de UX y validaciones del flujo de aplicacion publica en uis/website/application.
8. Migrar VALID_SUPPLY_CATEGORIES (ppe/wound_care/diagnostics/medications/consumables) a las categorias que pide el CONTEXT (medication/ppe/consumable/equipment) — brecha conocida, deliberadamente no resuelta en la instrumentacion de telemetria del 2026-08-14 por ser un cambio de modelo de datos mayor y no bloqueante.
9. Persistir tambien el resto de eventos que el backend emite sin pasar por POST /telemetry/events (api_error_response, direct_stock_edit_rejected, outbound_order_rejected). login_succeeded/login_failed se resolvieron el 2026-08-21; stock_threshold_triggered y supply_expiry_flagged el 2026-09-13 (pipeline de negocio, Parte 2), pasando la sesion de la ruta como `db_session`. `direct_stock_edit_rejected` y `outbound_order_rejected` pueden usar ese mismo patron (sus rutas ya tienen sesion). `api_error_response` sigue bloqueado: se emite desde el exception handler global, fuera del ciclo de dependencias.
10. Conectar los eventos en stream a alertas de verdad (ej. escalar stock_threshold_triggered a Marcus) — la tabla ya existe y tiene los datos, falta el consumidor.
11. Pipeline de negocio (Partes 1-3 hechas el 2026-09-13). Desde el Ticket #DEV-53 lo dispara cada noche el job nocturno (contenedor `scheduler`), que recalcula el último mes cerrado y cubre el cierre del día 1 y la reconciliación de eventos tardíos. Pendiente de despliegue: mover `DATABASE_URL` y los umbrales a blocks si se adopta un servidor de Prefect. Pendientes de datos: catálogo clínica→país (P5/P6; hoy el país viaja con el producto) y captura de `department` (P4).
12. Recomendación de ingesta, fuera del pipeline: índice único sobre `tags->>'eventId'` en `telemetry_events` con `ON CONFLICT DO NOTHING`, previa limpieza de duplicados. Hoy `POST /telemetry/events` no deduplica y un reintento tras timeout duplica eventos.
13. Cola de tareas (#DEV-55): construir y probar `docker compose up -d --build redis worker flower` cuando haya disco; valorar mover a Celery otras operaciones lentas (p. ej. `POST /api/incidents/analyze` si crece el CSV, guardando el archivo y encolando solo su ruta) y proteger Flower con `--basic-auth` si se expone fuera de localhost.

## Riesgos activos
- Dependencia operativa de API externa para CRUD.
- Inconsistencias de modelo entre respuestas del servicio remoto.
- Riesgo regulatorio si no se formalizan pronto trazabilidad y controles de acceso.
- Posible ralentizacion por deuda tecnica de tipado.
- Los eventos que el backend emite sin pasar por el endpoint HTTP (api_error_response, direct_stock_edit_rejected, outbound_order_rejected) siguen sin persistirse en telemetry_events: solo se loguean. Login, stock_threshold_triggered y supply_expiry_flagged ya se persisten. Ver punto 9 de "Proximos pasos".
- Pipeline de negocio: si se cambia una regla de `data/process/supply_performance_transforms.py` sin subir `TRANSFORM_VERSION` en `data/pipelines/pipeline.py`, durante una hora la transformación puede devolver de caché el resultado calculado con la regla vieja.
- Supabase en plan gratuito se duerme por inactividad, y el sintoma engaña: el pooler responde `tenant/user postgres.<ref> not found` y el dominio del proyecto ni resuelve en DNS, igual que si se hubiera borrado. Solo queda UN hueco libre (el otro es una app en produccion del usuario): la base del proyecto es `healthcore-data` (eu-central-1, ref `klaxalwsyrucwszvdkhu`) desde el 2026-09-24. `database-auditory` (eu-west-1) esta pausado a proposito; no restaurarlo sin pausar antes `healthcore-data`.
- Hay dos `.env` con `DATABASE_URL`: el de la raíz (lo usa `docker compose`) y `services/api/.env` (dev nativo). Si divergen, los contenedores ganan y usan el suyo. Ambos deben apuntar a `healthcore-data` (eu-central-1).
- Cola de tareas: la imagen `services/worker/Dockerfile` no se ha construido nunca (falta de disco el 2026-09-13); la primera vez que se levante `worker`/`flower` en Docker puede aparecer un fallo de build no detectado. Flower no tiene login: sus puertos solo se publican en `127.0.0.1`.
- `GET /telemetry/report` responde 500 (no 400) ante fechas mal formadas en `start_date`/`end_date`: `_parse_query_datetime` no captura el `ValueError`.

## Criterio de avance para la siguiente iteracion
- Sin errores criticos de lint en modulos clave.
- Contrato de datos unificado aplicado en dashboard, detalle y formularios.
- Checklist minimo de cumplimiento tecnico definido para nuevas features.
- Primer tablero de metricas de fase 1 acordado.